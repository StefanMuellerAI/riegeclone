# Optional — aktiviert den AI-Copiloten und Document Extraction mit Claude Opus 4.7.
# Ohne Key laufen beide Features in Demo-Fallback mit realistischen Mock-Antworten.
ANTHROPIC_API_KEY=

# Falls lokal ohne Docker gestartet wird:
DATABASE_URL=postgres://frachtwerk:frachtwerk@localhost:5433/frachtwerk
